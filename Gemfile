source "https://rubygems.org"

# Used only for GitHub Pages CI — local dev uses system jekyll directly
# See CLAUDE.md for local dev instructions
gem "github-pages", group: :jekyll_plugins
gem "webrick"
